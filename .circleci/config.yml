version: 2.1
jobs:
  build:
    docker:
      - image: node:16-alpine

    steps:
      - checkout
      - run: |
          docker login --username $DOCKER_USER --password-stdin $DOCKER_PW
          docker build -t diliniiresha/docker-myp -f Dockerfile.dev .
          docker run diliniiresha/docker-myp npm run test -- --coverage
workflows:
  version: 2
  build-deploy:
    jobs:
      - build

